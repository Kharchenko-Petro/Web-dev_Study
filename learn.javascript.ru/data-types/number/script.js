/* function sum() {
  requestNumber
  return a + b;
}

function requestNumber(request) {
  const number = prompt(request, 0);

  while ()
  if (checkIfCanceled(number)) {
    return NaN;
  } else {
    input
  }

  const numberTwo = inputCorrectNumber('Input second number');

  if (checkIfCanceled(numberTwo)) {
    return alert('You canceled input!');
  }

  return [num]
}

function inputCorrectNumber(request) {
  const number = prompt(request, 0);

  } else if( isFinite( +number ) ) {
    return number;
  } else {
    return inputCorrectNumber('Input correct number please');
  }
}

function checkIfCanceled(input) {
  if (input === '' || input === null) {
    return true;
  }

  return false;
} */

Math.floor(( Math.random() * (max - min + 1) ) + min); 